# 安全说明

## 支持范围

项目当前面向 1Panel V2 主节点和 systemd 环境。安装和每次运行都会重新检查必要路径、服务、数据库字段和 API。

如果 1Panel 后续版本改变相关 API 或数据库结构，脚本应停止并报告错误，不会猜测字段或自行替换面板文件。

## 凭据处理

- 脚本只在本机读取现有 1Panel API Key。
- API Key 仅用于请求 `127.0.0.1` 上的 1Panel API。
- API Key 不写入本项目配置，不输出到日志，也不发送至 GitHub 或其他外部服务。
- 脚本不会自动启用 API、生成 API Key 或修改 IP 白名单。

## 权限

安装、配置、升级、修复和卸载需要 root 权限，因为操作涉及 systemd 单元和 1Panel 本机数据库的只读访问。

项目不使用 Docker Socket，不安装特权容器，也不挂载宿主机根目录。

## 报告问题

普通错误可以提交 GitHub Issue。报告中不要包含：

- API Key、Cookie、密码或 Token；
- 1Panel 安全入口；
- 数据库完整内容；
- 包含凭据的 `.env`、Compose 文件或日志。

安全漏洞请使用 GitHub 的私密漏洞报告功能，不要公开发布利用细节或真实凭据。
